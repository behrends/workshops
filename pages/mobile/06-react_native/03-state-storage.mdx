import { Callout, Tabs } from 'nextra/components';

# Zustand mit state und AsyncStorage

<Callout>
  **Dauer:** 45 Minuten

  - state im `TextInput`
  - Speicherung mit `AsyncStorage`
</Callout>

Schritte:

- `TextInput` mit Zustand (_state_) austatten
- `Button` bestätigt state im alert?
- `AsyncStorage` installieren
- `AsyncStorage`: speichern mit Button
- `AsyncStorage`: Daten laden
- größere Übung: Todo löschen?