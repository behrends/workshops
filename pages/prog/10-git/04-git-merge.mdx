import { Callout } from 'nextra/components';

# Git-Konflikte lösen

<Callout type="info" emoji="👨🏻‍💻">
Infos für Dozent: 

- zweiter GitHub-Account
- clone
- config 
</Callout>

- `git pull`: Merge-Strategie in config festlegen (Terminal, VS Code)
- `git pull` mit auto merge (verschiedene Zeilen geändert)
- `git pull` mit Konflikt (gleiche Zeilen geändert)
- alles in Terminal und VS Code zeigen